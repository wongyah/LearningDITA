【任务说明】

在 `mkdocs.yml` 文件中，以 `nav` 中已有的目录格式为示例，将 ${new_folder} 文件夹中的文件添加到章节 ${heading1}/${heading2} 中。

如无特别说明，请将文件添加到指定章节的末尾。如果 `nav` 中还没有该章节，请在末尾添加新章节。

【本次任务变量】

- new_folder: ${new_folder}
- heading1: ${heading1}
- heading2: ${heading2} (可选) 默认为 `index.md` 文件的标题
